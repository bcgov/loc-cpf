<nav class="navbar navbar-dark bg-primary navbar-expand-lg  p-3">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Batch BC Geocoder</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "API Tokens", tokens_users_path, class: "nav-link #{'active' if current_page?(tokens_users_path)}" %>
        </li>
        <% if current_user.admin? %>
        <li class="nav-item">
          <%= link_to "Users", admin_users_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Jobs", admin_jobs_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Settings", admin_settings_path, class: "nav-link" %>
        </li>
        <% end %>
      </ul>
      <% if current_user.present? %>
      <%= link_to "#{current_user.idir_username}", user_profile_path, class: "btn btn-outline-secondary" %>
      <% end %>
    </div>
  </div>
</nav>